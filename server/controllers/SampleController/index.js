'use strict';

// Import the controller files here
const sampleApiRequest = require('./sampleApiRequest');

// Consolidate and export them as part of the `SampleController`
module.exports = {
  sampleApiRequest
};
